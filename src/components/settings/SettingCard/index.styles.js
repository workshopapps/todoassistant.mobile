import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  card: {
    paddingVertical: 10,
    backgroundColor: '#fff',
    borderRadius: 10,
    paddingHorizontal: 5,
    marginBottom: 10,
  },
});
