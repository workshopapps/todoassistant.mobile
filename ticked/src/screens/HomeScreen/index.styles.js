import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    paddingHorizontal: 10,
    flex: 1,
  },
  button: {
    flexDirection: "row",
    marginHorizontal: 2,
    padding: 10,
  }
});
