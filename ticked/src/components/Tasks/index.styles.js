import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        backgroundColor: "#fff",
        borderRadius: 10,
        borderColor: "#000",
        padding: 10,
        // alignItems: "center",
    }
})
